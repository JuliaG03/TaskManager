import java.util.List;

public class TaskList <T extends Task>{
    private List<T> taskList;

    //add, delete, update, search by name, search byi
    // , sort by name, sort by status, sort bypriority
    //show only done
    //show only to do
    //show only in progress

    //daca T e ShoppingTask - show all products, !!!!!!fa sort si search si acolo
}
